# Docker MCP gateway configuration placeholders.
# Keep the real .env.mcp file local and never commit it.
# Use the variable names expected by the configured Docker MCP catalog.

github.personal_access_token=<github-token>
trello.api_key=<trello-api-key>
trello.token=<trello-token>
trello.board_id=<trello-board-id>
